describe("test user controller", ()=> {
    const users = {
        4: {name: 'mark', email: 'mail'},
        5: {name: 'paul', email: 'mail2'},
    };

    const accounts = {
        'Mark': {userId:'mark'},
        'Paul': {userId:'paul'},
    };

    const transfers ={

    }
    // Put set up in here like mocking the DB / Data
    //import user import
    //function first test
    test("Create a new bank account for a customer, with an initial deposit amount", () =>  {

    })

    test("Create multiple accounts for a user", () =>  {

    })

    test("transfer amounts between two accounts of a user", () =>  {

    })

    test("transfer amounts between two users", () =>  {

    })

    test("Retrieve Balance", () =>  {

    })

    test("Retrieve transfer history for an account for a year", () =>  {

    })
    // test transfer logic

    // take a fun() => creates a user

    //when user.controller.create() => func() => called with correct args

    // function calling to check persistance also called with the full name
})